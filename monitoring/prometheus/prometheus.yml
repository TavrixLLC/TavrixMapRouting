global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: tavrix-routing-api
    scrape_interval: 5s
    static_configs:
      - targets: ["routing-api:3000"]
